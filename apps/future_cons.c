#include <future.h>
#include <xinu.h>

uint cons() {
  /*
  int i, status;
  status = future_get(f, &i);
  if (status < 1) {
    printf("failed\n\r");
      return -1;
  }
  printf("consumed: %d\n\r", i);
  return OK;
  */
}
