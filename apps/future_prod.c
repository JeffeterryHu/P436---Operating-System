#include <future.h>
#include <xinu.h>

uint prod() {
		/*
		int i, j;
		j = (int)f;
		for (i=0; i<1000; i++) {
				j += i;
		}
    printf("produced:%d\n\r",j);
		future_set(f, &j);
		return OK;
		*/
}
